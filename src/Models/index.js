const Section = require('./Section.model');
const PictureCard = require('./PictureCard.model');
const TextCard = require('./TextCard.model');
const LogoCard = require('./LogoCard.model');

module.exports = {
  Section, PictureCard, TextCard, LogoCard,
};
